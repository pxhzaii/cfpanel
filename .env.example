# CF Panel 环境变量示例
# 复制为 .env 并按需修改（本地开发时使用；部署到 Cloudflare Pages 请使用页面设置里的环境变量）

# 面板用户列表（必填，JSON 数组格式，如 [{"username":"admin","password":"你的密码"}]
PANEL_USERS=[{"username":"admin","password":"your-secret-password"}]

# Cloudflare API Token（必填，权限范围见 README）
CF_API_TOKEN=your-cf-api-token

# GitHub Token（可选，用于"重新部署"功能推 commit 触发 GitHub webhook 部署，需 repo 权限）
GH_TOKEN=your-github-token